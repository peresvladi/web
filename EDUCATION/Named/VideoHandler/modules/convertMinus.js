const convertMinus = (negativeValue, arrayPull) => {
    let plusValue = (arrayPull.length + negativeValue);
    return plusValue;
}
// функция convertMinus - преобразует отрицательно значение индекса при работе с массивом в положительное
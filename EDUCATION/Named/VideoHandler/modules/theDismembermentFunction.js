const theDismembermentFunction = (incomingBody, separator) =>{
const dismemberment = incomingBody.split(separator);
return dismemberment;
// theDismembermentFunction - функция которая принимает в качестве аргуменов первое: строку(incomingBody), второе разделитель (sepfrator). В перовой строке своего тела вошедшая строка(incomingBody) разбивает по метке разделителя (sepfrator). Во-второй строке полученный массив элеменов возвращается оператору вызвавшему эту функцию (theDismembermentFunction).

}
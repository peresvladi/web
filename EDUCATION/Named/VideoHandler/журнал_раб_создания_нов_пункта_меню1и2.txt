function inputVideo()

function listNamberOne(a = meny1())
377 function meny1()
378 document.write(`<select name="top" id="top">`); //-пишет
379 for (let i = 0; i < window.localStorage.length; i++) {
380 if (window.localStorage.key(i).charAt(0) === "0") { // выбирает с нулем в первом символе слова.
381 let nameValueВutton = window.localStorage.key(i); // при нахождении ключа с нулем в первом символе присваиваем имя этого ключа переменной 
382 document.write(`<option value='${nameValueВutton}'>'${nameValueВutton.slice(1).replace("0", " * ")}'</option>`); //прописываем найденное значение в value и в подпись кнопки с адаптацией
383 if (nameValueВutton.charAt(1) === "0") { //проверяет второй символ ключа с начала, если нет второго 0, тогда:
379 for (let i = 0; i < window.localStorage.length; i++) {
380 if (window.localStorage.key(i).charAt(0) === "0") {
381 let nameValueВutton = window.localStorage.key(i);
382 document.write(`<option value='${nameValueВutton}'>'${nameValueВutton.slice(1).replace("0", " * ")}'</option>`);
383 if (nameValueВutton.charAt(1) === "0") { // проверяяет, если вторая позиция с начала 0 тогда:
384 document.getElementById("top").value = nameValueВutton; // тогда присваивает значение списку top
379 for (let i = 0; i < window.localStorage.length; i++) {






